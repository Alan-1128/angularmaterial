import { InputPersonalizeDirective } from './input-personalize.directive';

describe('InputPersonalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPersonalizeDirective();
    expect(directive).toBeTruthy();
  });
});
